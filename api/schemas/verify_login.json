{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VerifyLoginResponse",
  "description": "Schema for POST /verifyLogin response payload.",
  "type": "object",
  "required": ["responseCode", "message"],
  "properties": {
    "responseCode": { "type": "integer" },
    "message": { "type": "string" }
  }
}

