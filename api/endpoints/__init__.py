"""Endpoint-specific service classes (analogous to Page Objects for UI).

Add one module per resource here, e.g. ``user_api.py``, ``order_api.py``.
Each service should accept a :class:`api.base_client.BaseApiClient` in its
constructor and expose clean, intention-revealing methods.
"""


